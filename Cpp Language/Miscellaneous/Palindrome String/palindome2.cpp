#include <iostream>
#include <string>

using namespace std;

bool palindrom(string);

int main()
{


    cout.setf(ios_base::boolalpha);
    cout << palindrom("dadsd");

}

bool palindrom(string napis)
{
    if (napis[i] != napis[length - 1 - i])
    size_t length = napis.size();

    for (int i = 0; i < length/2; i++)
    {
        
            return false;
    }

    return true;
}